import React from "react"
import {HERO_IMAGE_CONTAINER} from "../helper/common"

export default class CardHero extends React.Component {
    render() {
        return (
            <div className={HERO_IMAGE_CONTAINER}>
                <img src="" alt="hero"/>
            </div>
        )
    }
}
